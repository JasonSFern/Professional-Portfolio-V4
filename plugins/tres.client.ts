// TresJS doesn't need a global plugin in Nuxt
// Components are auto-imported when you use them
// This file can be removed or kept as placeholder

export default defineNuxtPlugin(() => {
  // TresJS components will be available globally via auto-import
})
